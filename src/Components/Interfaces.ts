export interface IApiCall {
    userId: number;
    id: string;
    title: string;
    body: string
}

export interface IApiCallWithId {
    userId: number;
    id: string;
    title: string;
    body: string
}
